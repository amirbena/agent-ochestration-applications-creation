# CI Demo Notes   

This file exists only to demonstrate a CI failure and its fix as part of the CI demo
flow described in the ci-demo-agent PRs. It has no bearing on any production Agent.

## Purpose

This note intentionally starts with one line that has trailing whitespace, which the
repository's Markdown validator (`scripts/validate_repository.py`) rejects. A follow-up
commit removes it once the CI failure has been demonstrated.
